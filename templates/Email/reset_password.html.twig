<p>Bonjour <b>{{ user.username }}</b>,</p>

<p>Pour lancer le processus de r&eacute;initialisation du mot de passe de votre compte Ch&egrave;que Eco-Energie <b>"{{ user.username }}"</b>, cliquez sur le lien ci-dessous :</p>
<br>

{% set confirmationUrlDisplay = app_is_https ? confirmationUrl|replace({'http:': 'https:'}) : confirmationUrl %}
<a href="{{ confirmationUrlDisplay }}">{{ confirmationUrlDisplay }}</a>

<br><br>
<p>Si ce lien ne fonctionne pas, copiez l'URL, puis collez-la dans une nouvelle fen&ecirc;tre de navigateur.</p>

<p>Attention ! Ce lien n'est valable que 1 heure apr&egrave;s l'envoi de ce message et &agrave; condition que vous n'ayez pas effectu&eacute; une nouvelle demande de r&eacute;initialisation de mot de passe.</p>

<p>Si vous avez re&ccedil;u ce message par erreur, il est possible qu'un autre utilisateur ait saisi votre adresse e-mail par inadvertance alors qu'il tentait de r&eacute;initialiser un mot de passe.</p>

<p>L'&eacute;quipe Ch&egrave;que Eco-Energie de la R&eacute;gion Normandie.</p>