<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">

        <header>
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 header-container">
                <div class="hidden-xs col-sm-3 col-md-3 col-lg-3">
                    <div id="bloc_logo">
                        <a href="{{ path('demande_list_all') }}">
                            <img
                                    src="{{ asset('build/images/backOffice/logo/logo_Normandie.jpg') }}"
                                    alt="logo"
                                    class="header-logo"
                            >
                        </a>
                    </div>
                </div>

                <div class="hidden-xs col-sm-6 col-md-6 col-lg-6 bloc_admin_menu text-center">
                    <div class="main_title">
                        <img
                                src="{{ asset('build/images/backOffice/logo/logo_espace_partenaire.png') }}"
                                alt="logo"
                                class="header-logo-espace"
                        >
                    </div>

                    {% if app.user.roles is defined %}
                        {% if ('ROLE_CONSEILLER' in app.user.roles or 'ROLE_ADMIN' in app.user.roles)
                            and is_granted("IS_AUTHENTICATED_REMEMBERED")
                        %}
                            {% block admin_menu %}
                                {{ include('BackOffice/Common/menu_conseiller.html.twig', {'dataMenuConseiller': dataMenuConseiller}) }}
                            {% endblock %}
                        {% endif %}
                    {% endif %}
                </div>

                {% if app.user.roles is defined %}
                    {% if ('ROLE_CONSEILLER' in app.user.roles
                    or 'ROLE_INSTRUCTEUR' in app.user.roles
                    or 'ROLE_INSTRUCTEUR_UP' in app.user.roles
                    or 'ROLE_AUDITEUR' in app.user.roles
                    or 'ROLE_RENOVATEUR' in app.user.roles
                    or 'ROLE_EPCI' in app.user.roles
                    or 'ROLE_CLIENT' in app.user.roles
                    or 'ROLE_ADMIN' in app.user.roles
                    or 'ROLE_TECHNIQUE' in app.user.roles
                    or 'ROLE_SUPER_ADMIN' in app.user.roles
                    ) and is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <div class="col-xs-12 col-sm-3 col-md-3 col-lg-3 header-login">
                        Bonjour <span class="bold"> {{ app.user.username }} &nbsp;</span>
                        &nbsp;
                        <a href="{{ path('admin_user_logout') }}"
                           class="btn btn-warning btn-xs"
                           role="button"
                           data-toggle="tooltip" data-placement="bottom"
                           data-container="body"
                           title="Se d&eacute;connecter"
                        >
                            <i class="glyphicon glyphicon-log-out"></i>
                        </a>
                    </div>
                    {% endif %}
                {% endif %}
            </div>
        </header>

    </div>
</nav>
