<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 bloc" id="bloc-statut">
    <fieldset>
        <legend class="marginTop20"><span class="legend-icon"></span>&nbsp;Statut</legend>

        <div class="row">
            <label class="col-xs-12 col-sm-12 col-md-5 col-lg-5 control-label">Date de rattachement :</label>
            <div class="col-xs-12 col-sm-12 col-md-7 col-lg-7">
                {{ dateRattachement }}
            </div>
        </div>

        <div class="row">
            <label class="col-xs-12 col-sm-12 col-md-5 col-lg-5 control-label">Actif :</label>
            <div class="col-xs-12 col-sm-12 col-md-7 col-lg-7">
                {% if 0 == partenaire.partenaire_statut.enabled %}
                    {% set statut = 'Inactif' ~ ' depuis le ' ~ dateInactif %}
                {% elseif 1 == partenaire.partenaire_statut.enabled %}
                    {% set statut = 'Actif' %}
                {% endif %}
                {{ statut }}
            </div>
        </div>

    </fieldset>

    <hr class="bloc-limit">
</div>
